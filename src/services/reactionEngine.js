//define estados e condições específicas para ações de reação

/* Habilidade Aparo Avassalador
if (
  action.type === 'BLOCK' &&
  successMargin >= 10
) {
  triggerReaction({
    type: 'COUNTER_ATTACK',
    source: character
  });
}
 */